import { Metadata } from "next";

export const metadata: Metadata = {
  title: "reported machines",
  description: "reported machines",
};

export default function ReportedMachines() {
  return (
    <div>
      <h1>Reported machines</h1>
    </div>
  );
}